# CLAUDE.md

ポケモンチャンピオンズ（レギュレーションM-B シングル）の対面ダメージ表を生成するプロジェクト。
対戦中にスマホやサブモニタで開いて、相手が出てきた瞬間に打点と被弾を確認するためのもの。

## 構成

```
data/dex.csv               図鑑。種族値・タイプ・特性（一次データ）
data/moves.csv             技データ（一次データ）
data/type_chart.csv        タイプ相性表（一次データ）
data/ポケモン図鑑.xlsx      CSVの移行元。コードはもう読まない。消さずに残してある
data/技使用率データ.JSON     使用率230体分（pkmnchamps.com のAPIレスポンス。月ごとに差し替える）
data/move_names_en_ja.json 技使用率データ.JSON の英語技名 → 日本語技名の対応表（一次情報）
data/特性の効果.pdf         育成考察Wikiの特性一覧ページ。特性の効果を確認したいときの原典
data/abilities_ja.json     上のPDFから抽出した特性名 → 効果（240件）。計算には使わない参照用
build/extract_abilities.py 特性の効果.pdf → abilities_ja.json の変換。PDFを更新した時だけ流す
party.txt                  自分のパーティ定義。ゲーム内のステータス画面を見ながらここを編集する
build/engine.py             データ読み込み・実数値計算・タイプ相性・特性
build/party.py              party.txt を読んで PARTY を組み立てるローダー。直接は編集しない
build/generate.py           相手の型を組む処理とダメージ計算。整合性チェックの入口
build/export_app_data.py    ブラウザが読む appdata/*.json を書き出す
build/verify_engine.js      JS移植がPython版と同じ数値を出すか確認する（node で実行）
appdata/*.json              生成物。ブラウザ用のデータ。直接編集しない
index.html                  ダメージ表。登録したパーティで計算する
party.html                  パーティー登録画面。取り込み・調整・書き出し
assets/engine.js            ダメージ計算（Python版からの移植）
assets/app.js               ダメージ表の描画
assets/party.js             パーティー登録画面
assets/store.js             登録したパーティの保存（localStorage）
assets/style.css            共通のCSS
```

ビルドと検証:

```bash
pip install openpyxl
python build/generate.py         # データの整合性を確認（生成物は作らない）
python build/export_app_data.py  # appdata/*.json を書き出す
node build/verify_engine.js      # JS移植がPython版と一致するか確認
```

`appdata/` を作り直したらコミットすること。CI が「コミット済みのものと一致するか」を見ている。

パーティの構成を変えるときは `party.txt` をゲーム内のステータス画面を見ながら編集する
（`build/party.py` は直接触らない）。書き方は `party.txt` 冒頭のコメントを参照。
能力ポイントは実数値・種族値・性格から自動で逆算され、入力ミスはビルド時にエラーで止まる。

パーティは `party.html`（ブラウザ上の登録画面）でも編集できる。登録内容は localStorage に
`party.txt` と同じテキストのまま入る。リポジトリの `party.txt` は未登録時の既定値。

**`appdata/*.json` は生成物。直接編集しても次の書き出しで消える。**

公開先: https://cola-jp.github.io/Champion-VS/ （main ブランチのルートを GitHub Pages が配信）

## 絶対に間違えてはいけないドメイン知識

過去に実際にやらかした間違いなので、変更のたびに確認すること。

### メガシンカ後の実数値

**ゲーム内のステータス画面はメガシンカ前の数値を表示する。** メガ形態の計算には必ずメガ側の種族値を使う。

| | 表示（メガ前） | 実際（メガ後） |
|---|---|---|
| ギャラドス A | 177 | **207** |
| ギャラドス B | 100 | **130** |
| ギャラドス D | 120 | **150** |
| チルタリス B | 110 | **130** |
| チルタリス C | 134 | **178** |

`build/party.py` の `EXPECTED`（generate.py 側）に期待値を置いてあり、ビルド時に自動検証する。
ここで落ちたら `ev` か `nature` の設定ミス。

### 努力値の表記

このゲームは「能力ポイント」制。**1ポイント = 努力値8**、1体あたり合計66ポイント、1ステータス最大32ポイント。
`party.py` の `ev` はポイント表記であって努力値ではない。

### ダメージ計算の丸め順

`基礎 → 乱数 → タイプ一致 → タイプ相性 → その他補正` の順に、各段階で切り捨てる。
順番を変えると1〜2ずれて乱数判定が変わる。`engine.damage()` を勝手に書き換えないこと。

### 特性の扱い

- **かたやぶり**（メガギャラドス・ドリュウズ）は、あついしぼう・マルチスケイル・ふゆう等の
  防御特性を全て無視する。`ability_mod(mold_breaker=True)` が担当。
- **マルチスケイル**は満タン時のみ0.5倍。カイリューは「皮あり／皮なし」の2行に分けて両方出す。
  かたやぶり側の駒はどちらの行でも同じ数値になる（これが正しい）。
- **ばけのかわ**は倍率ではなく1回無効。ダメージは等倍で出し、**判定だけ1発ぶんずらす**
  （確1→確2、乱1→乱2。`engine.verdict_plus_one()`）。かたやぶりなら皮を無視するのでずらさない。
  自分がばけのかわ持ちのときは、被弾の主表示を「皮が剥がれた後」の数字にする。
  0%を主表示にしても実戦で役に立たないため。
- **へんげんじざい／リベロ**は発動すると必ずタイプ一致が乗る。ただし発動していない
  （＝不一致の技を撃つ）場合もあるので、マルチスケイルと同じく「発動」「未発動」の
  2行に分けて両方出す。`build_threats()` の `protean` がその分岐。
- **フェアリースキン**（メガチルタリス）はノーマル技をフェアリーに変え、威力1.2倍。
  タイプ一致も乗るのでハイパーボイスは実質 1.2 × 1.5。
- **きれあじ**（エルレイド）は斬撃技のみ1.5倍。対象は `party.SLASH_MOVES`。

### 相手の特性の扱いは必ず明示的に決める

`build/generate.py` の `ABILITY_HANDLING` に、表に出てくる相手の特性を全部並べて、
「反映済み」「影響なし」「未反映（理由つき）」のどれなのかを1行ずつ書いてある。
使用率データを差し替えて**ここに無い特性が出てくるとビルドが警告する**。
判断しないまま表を出さないための仕掛けなので、警告が出たら必ず1行足すこと。
効果を調べるときは `data/abilities_ja.json`（原典は `data/特性の効果.pdf`）を引く。

`their_hit()` は**相手の攻撃特性を反映する**（採用率が最も高い特性＝`threat['ability']`）。
かたいツメ・ちからもち／ヨガパワー・てきおうりょく・テクニシャン・きれあじ・へんげんじざい。
補正の掛け方は `my_hit` と同じ段階に揃えること（威力と攻撃は基礎、一致は stab、残りは extra）。

**かたいツメは接触技だけ1.3倍。** 物理でも じしん・いわなだれ のように接触しない技があるので、
規則で決め打ちせず `party.CONTACT_MOVES` / `NON_CONTACT_MOVES` に技ごとに書く。
かたいツメ持ちが未分類の技を使っているとビルドが警告する。

**自軍の防御特性も反映する。** あついしぼう・ふゆう・マルチスケイルなどが被弾に効く。
相手がかたやぶり系（かたやぶり・ターボブレイズ・テラボルテージ）なら全て無視される。
**条件で剥がれる特性（`engine.STRIPPABLE_ABILITIES`）は剥がれた後の数字も併記する**:
マルチスケイルは主表示が満タン時で、解除時を副行に出す。ばけのかわは主表示を
剥がれた後にして印を付ける。剥がれると最大打点の技そのものが変わりうるので、
`their_hit` は特性を効かせた場合と切った場合で技の選び直しから2回計算している。

**まだ入れていないもの:**
きもったま、バトルスイッチ。`ABILITY_HANDLING` に理由つきで書いてある。

### 使用率データの打ち切り

JSONの `spreads` は各ポケモン**上位12件しか入っていない**。合計は平均71.5%（最小43.8%）にしかならず、
細かい配分が大量に切り捨てられている。`natures` と `items` は上限10件だが選択肢が少ないので
合計はほぼ100%になる。

そのため型の比率は**報告分の合計で割り直した値**を表示している。「全体の何%」ではなく
「判明している配分の中での比率」。この区別を崩さないこと。

### 型の分割ルール

投資量の多い**上位2ステータス**が同じものを1パターンとして統合する。
CS型とSC型のような順序違いは同一視する（これを別物として数えて票が割れるバグを踏んだ）。
第3ステータスへの端数振り（HAS型 vs AS型など）も統合する。

### メガ形態の判定

`name.startswith('メガ')` で判定してはいけない。**メガニウム**のように名前がたまたま
「メガ」で始まる普通のポケモンを誤ってメガ形態とみなし、実際に37位がメガメガニウムではなく
メガニウムの種族値・タイプで計算されていた。判定は `engine.is_mega()` を通すこと。
同じ図鑑番号の中に「メガ」を外した名前が居るときだけメガ形態とみなす。

逆に、単純に「メガ」を外して照合するだけだと**メガフラエッテ**が漏れる。図鑑には
`フラエッテ(えいえん)` しか無く、通常のフラエッテが存在しないため。括弧書きを外して照合し、
リージョン絞り込みで候補から外れる分は `engine.REGION_FORM_MEGA` の例外で拾い直している。
ライチュウ(アローラ)・ヤドラン(ガラル)は通常形態が別に居てメガはそちらのものなので、
「リージョンフォームにメガを付ける」を一般ルールにしないこと。

### リージョンフォーム

JSONの各エントリには `region_form` フィールドがある（`samurott-hisui`、`rotom-wash` など）。
図鑑番号だけで形態を決めると、**ヒスイダイケンキが通常ダイケンキの種族値で計算される**。
実際にこのバグを出した。形態の解決は必ず `engine.resolve_form()` を通すこと。

図鑑側の表記は種によって揺れる（`ダイケンキ(ヒスイ)` と `ウォッシュロトム`）ので、
`REGION_KEYWORD` のキーワード照合で吸収している。

### 新しいポケモン・技・特性を足すとき

一次データは全部テキスト（CSV）なので、Excel は要らない。差分が見えるので取り込みミスに気づける。

- **ポケモン** … `data/dex.csv` に1行。`no,name,type1,type2,abilities,hp,atk,def,spa,spd,spe`。
  単タイプなら `type2` は空。**特性は `/` 区切り**（`しんりょく/ようりょくそ`）。
  メガ形態やリージョンフォームは同じ `no` で別行にする（`メガ○○`、`○○(ヒスイ)`）。
- **技** … `data/moves.csv` に1行 + `data/move_names_en_ja.json` に英語名との対応。
  使用率データは英語名なので、対応表に足さないとその技が計算から抜ける（警告が出る）。
  列は `name,type,category,power,accuracy,priority,effect,rank_change`。
  **一撃必殺技は `effect` に「一撃必殺」と書けば拾われる**（技名を並べた表は無い）。
  **積み技は `rank_change` から判定する**。書式は `自分こうげき+2`、複数なら
  `自分とくこう+1/とくぼう+1`（2つ目以降は自分/相手を省略して直前を引き継ぐ）。
  末尾に `(20%)` のような条件が付いていると確実に積めないので積み技として数えない。
- **特性** … 名前は `dex.csv` に書けば済むが、**計算に効くかどうかは別に決める**必要がある。
  `generate.py` の `ABILITY_HANDLING` に1行足す（未分類だと警告が出続ける）。
  効果の説明は `data/abilities_ja.json` を引く。載っていない特性もあるので、その場合は自分で調べる。

タイプ名の打ち間違いや種族値の欠けは `engine._check_dex()` が名前を挙げて止める。

### 連続技は合計ダメージで計算する

スケイルショットやトリプルアクセルは、基礎威力1発ぶんで計算してはいけない。
以前はそうなっていて、**マスカーニャのトリプルアクセルがチルタリスに179%（確定1発）入るのに
「ふいうち 39%」と表示していた**（こおりがドラゴン/ひこうに4倍、それが3連撃）。

- 何回当たるかは効果欄の「2〜5回連続攻撃」「3回連続攻撃」から導く（`engine.parse_multi_hit`）。
- トリプルアクセルのように「当たるごとに威力が20ずつ増加」するものは `step` に増分が入る。
- **1発ずつ `damage()` を通して足すこと**（`engine.multi_damage`）。各発で切り捨てが入るので、
  威力を合算して1回で計算すると数値が合わない。
- 幅は「最低回数×最低乱数 〜 最高回数×最高乱数」。何発ぶんの数字か分からないと桁を読み違えるので、
  表示には必ず回数のタグを出す。

### 一撃必殺と積み技は技名を並べない

どちらも技データから導く。`party.OHKO_MOVES` と `party.BOOSTING_MOVES` は
`data/moves.csv` を読んで組み立てた集合で、技名の直書きはもう無い。
新しい一撃必殺技や積み技が増えても、CSVに正しく書けば勝手に効く。

**積み技の段階数は技ごとに違う。** つるぎのまいは攻撃+2なので2.0倍、
りゅうのまいは攻撃+1なので1.5倍。以前は技を問わず攻撃・特攻を一律1.5倍していて、
+2の技を過小評価していた。倍率は `engine.rank_multiplier()`、
どの能力が何段階上がるかは `engine.self_boost()` が返す。
上げるのが防御や素早さだけの技（てっぺき・こうそくいどう）は積み技に数えない。
JS側は解析を書き直さず、`rules.boostStages` に入れた結果を引くだけにしてある。

### 特性欄は連結ではなく `/` 区切り

以前は xlsx で `ふくつのこころきれあじせいぎのこころ` のように区切り無しで連結されていて、
分割できないので部分一致でしか判定できなかった。CSV 移行で `/` 区切りに直してあり、
`DEX[name]['ab_list']` が正しい一覧。`DEX[name]['ab']` は連結した文字列で、
既存の部分一致（`'あついしぼう' in ab`）がそのまま動くように残してある。
**新しく特性を判定するコードを書くときは `ab_list` を使うこと。**

### 技名の誤り

`data/ポケモン図鑑.xlsx` には英語名をそのまま音写した誤りが混じっている。
判明分は `engine.MOVE_NAME_FIX` で読み込み時に補正する（技データと使用率シートの両方に適用）。

| xlsx の表記 | 正しい和名 |
|---|---|
| スピリットブレイク | ソウルクラッシュ |
| うでずもう | アームハンマー |

新しい誤りを見つけたらここに追加する。xlsx 側を直せるならそちらが本筋。

### メガ形態と配分の対応

メガ率が100%未満のポケモンで、**メガと逆向きの攻撃方向に振っている型は非メガ運用**とみなす。
カイリューが実例で、メガは A124 < C145 の特殊寄り、非メガは A134 > C100 の物理寄り。
A振り型の比率26%がドラゴナイト非採用率26%と一致する。`generate.build_threats()` に実装済み。

## 表示のルール

- **技の選び方**: 判定（確1 > 乱1 > 確2 …）が最も良い技を主表示する。同判定なら
  `DRAWBACK_MOVES`（はかいこうせん）を避けて他の技を選ぶ。ハイパーボイスで確定1発が取れる相手に
  はかいこうせんを出さないため。2行目に次善の選択肢を出し、判定が上がるなら水色にする。
- **ダメージバー**: 相手のHP全体を幅100%として、濃い部分が最低乱数、薄い部分が最高乱数。
  中央の線が50%。数字を読まずに確1／確2を判断するための主要素なので、壊さないこと。
- **被弾100%以上は赤字**。確定1発される相手を即座に見分けるため。
- **タイプ変換のタグ（`→フェアリー` など）は出さない。** 以前フェアリースキン用に出していたが、
  不要と判断して削除した。メガソーラーのウェザーボール（ノーマル→ほのお）も同様にタグは出さず、
  計算にだけ反映する。復活させないこと。
- 天候・いかく・積みは計算に**含めない**（積み技だけは各行に「技名+段階」を併記）。
  含めない方が素の打点が分かって使いやすい、と実戦で確認済み。
- **ステルスロックだけは例外で、ヘッダの「SR設置済み」トグルで表全体を切り替える。**
  自分が撒いた前提の表と撒いていない表を**両方とも静的に書き出し**、CSSのクラス付け替え
  （`body.sron`）で見せ分ける。**JSでダメージを計算し直さないこと** — JSが動かない環境と
  数値が食い違う。実装は `generate.sr_damage()` と `generate.render_row(hp_eff=...)`。
  - SRダメージは `int(最大HP × いわ相性 / 8)`。マジックガードは無効。**ひこうタイプにも入る**
    （まきびしと違い接地不要）。まきびしを足すなら 1/8・1/6・1/4 で、ひこう・ふゆうには
    入らないので条件が違う。混同しないこと。
  - SR側は判定・%・バーをすべて**残りHPに対する値**にする。こうしないと「バーが満タン＝倒せる」
    が成り立たなくなり、バーの役割が壊れる。
  - HPが減ると主表示する技自体が変わりうる（弱い技で確1が取れる）ので、タグを足すのではなく
    行をまるごと作り直している。切り替えても値が変わらない行は1本にまとめて出力する
    （全行を二重に出すとファイルが倍近くになるため）。
  - 相手側のHPにのみ適用。自分側の被弾%は未計算（相手が撒くかは事前に分からないため後回し）。

## 計算がPythonとJSの2箇所にあること

登録したパーティに応じてブラウザ側で計算するようになったので、ダメージ計算は
**Python（`build/generate.py`）と JS（`assets/engine.js`）の両方に存在する**。
これは二重管理だが、どちらも必要:

- Python … 相手の型を組む処理（配分の集約・メガ判定・リージョンフォーム・行分割）と、
  xlsx/JSON の読み込み。ブラウザには結果だけ `appdata/*.json` で渡す。
- JS … 登録されたパーティに対する計算。パーティが変わるたびに動くのでブラウザ側に要る。

**片方だけ直すと必ず食い違う。** それを検出するために `appdata/golden.json` に
Python で計算した期待値（現行 party.txt の全行）を置いてあり、
`node build/verify_engine.js` で全件突き合わせる。CI でも流している。
数字に関わるコードを触ったら、必ず `export_app_data.py` → `verify_engine.js` の順に通すこと。

移植で踏みやすい違い:

- Python の `int()` は0方向への切り捨て。JS は `Math.trunc`（`Math.round` ではない）。
- **Python の `round()` はちょうど .5 を偶数側に寄せる。** JS の `Math.round` は切り上げなので、
  そのまま使うと %表示が1ずれる。`Engine.pyRound()` を使うこと。
- JSON にすると `1.0` が `1` になる。倍率の表示は `mult()` で桁を戻している。
- 特性テーブルは「先頭から順に最初に一致したものを返す」ので、順序が変わると挙動が変わる。
  JSON では配列で渡して順序を保っている。

## 前提が変わった経緯

以前は「iOSでローカルHTMLのJSが動かない環境がある」ため、全カードを静的に書き出して
JSなしで読める構造を保っていた。パーティー登録機能を入れるにあたってこの前提は破棄され、
JSの使用と複数ファイル構成が許可された。**JSなしで読める状態を保つ必要はもう無い。**

## コーディングの約束

- 出力は日本語。ポケモン名・技名・特性名も日本語で統一する。
- 外部CDNを使わない（オフラインで開くため）。ライブラリは足さない。
- 数値を変えたら `python build/generate.py` で実数値の自動検証を通し、
  `python build/export_app_data.py` → `node build/verify_engine.js` で
  JS版と一致することを確認する。
- 表示を変えたときは、ブラウザで実際に開いて確かめる。
