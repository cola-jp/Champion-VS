# CLAUDE.md

ポケモンチャンピオンズ（レギュレーションM-B シングル）の対面ダメージ表を生成するプロジェクト。
対戦中にスマホやサブモニタで開いて、相手が出てきた瞬間に打点と被弾を確認するためのもの。

## 構成

```
data/ポケモン図鑑.xlsx      種族値・タイプ相性表・技データ（手作業で整備した一次データ）
data/技使用率データ.JSON     使用率230体分（pkmnchamps.com のAPIレスポンス。月ごとに差し替える）
data/move_names_en_ja.json 技使用率データ.JSON の英語技名 → 日本語技名の対応表（一次情報）
data/特性の効果.pdf         育成考察Wikiの特性一覧ページ。特性の効果を確認したいときの原典
data/abilities_ja.json     上のPDFから抽出した特性名 → 効果（240件）。計算には使わない参照用
build/extract_abilities.py 特性の効果.pdf → abilities_ja.json の変換。PDFを更新した時だけ流す
party.txt                  自分のパーティ定義。ゲーム内のステータス画面を見ながらここを編集する
build/engine.py             データ読み込み・実数値計算・ダメージ計算
build/party.py              party.txt を読んで PARTY を組み立てるローダー。直接は編集しない
build/generate.py           index.html を生成するビルドスクリプト
build/style.css             生成物に埋め込まれるCSS
build/app.js                生成物に埋め込まれるJS（検索の絞り込みと表示トグルのみ。計算はしない）
index.html                  生成物。GitHub Pagesで公開される。直接編集しない
```

パーティの構成を変えるときは `party.txt` をゲーム内のステータス画面を見ながら編集する
（`build/party.py` は直接触らない）。書き方は `party.txt` 冒頭のコメントを参照。
能力ポイントは実数値・種族値・性格から自動で逆算され、入力ミスはビルド時にエラーで止まる。

ビルド:

```bash
pip install openpyxl
python build/generate.py
```

`index.html` は毎回まるごと上書きされる。**生成物を直接編集しても次のビルドで消える。**

公開先: https://cola-jp.github.io/Champion-VS/ （main ブランチのルートを GitHub Pages が配信）

## 絶対に間違えてはいけないドメイン知識

過去に実際にやらかした間違いなので、変更のたびに確認すること。

### メガシンカ後の実数値

**ゲーム内のステータス画面はメガシンカ前の数値を表示する。** メガ形態の計算には必ずメガ側の種族値を使う。

| | 表示（メガ前） | 実際（メガ後） |
|---|---|---|
| ギャラドス A | 177 | **207** |
| ギャラドス B | 100 | **130** |
| ギャラドス D | 120 | **150** |
| チルタリス B | 110 | **130** |
| チルタリス C | 134 | **178** |

`build/party.py` の `EXPECTED`（generate.py 側）に期待値を置いてあり、ビルド時に自動検証する。
ここで落ちたら `ev` か `nature` の設定ミス。

### 努力値の表記

このゲームは「能力ポイント」制。**1ポイント = 努力値8**、1体あたり合計66ポイント、1ステータス最大32ポイント。
`party.py` の `ev` はポイント表記であって努力値ではない。

### ダメージ計算の丸め順

`基礎 → 乱数 → タイプ一致 → タイプ相性 → その他補正` の順に、各段階で切り捨てる。
順番を変えると1〜2ずれて乱数判定が変わる。`engine.damage()` を勝手に書き換えないこと。

### 特性の扱い

- **かたやぶり**（メガギャラドス・ドリュウズ）は、あついしぼう・マルチスケイル・ふゆう等の
  防御特性を全て無視する。`ability_mod(mold_breaker=True)` が担当。
- **マルチスケイル**は満タン時のみ0.5倍。カイリューは「皮あり／皮なし」の2行に分けて両方出す。
  かたやぶり側の駒はどちらの行でも同じ数値になる（これが正しい）。
- **ばけのかわ**は倍率ではなく1回無効。ダメージは等倍で出し、必要ならターン数に+1する。
- **へんげんじざい／リベロ**は発動すると必ずタイプ一致が乗る。ただし発動していない
  （＝不一致の技を撃つ）場合もあるので、マルチスケイルと同じく「発動」「未発動」の
  2行に分けて両方出す。`build_threats()` の `protean` がその分岐。
- **フェアリースキン**（メガチルタリス）はノーマル技をフェアリーに変え、威力1.2倍。
  タイプ一致も乗るのでハイパーボイスは実質 1.2 × 1.5。
- **きれあじ**（エルレイド）は斬撃技のみ1.5倍。対象は `party.SLASH_MOVES`。

### 相手の特性の扱いは必ず明示的に決める

`build/generate.py` の `ABILITY_HANDLING` に、表に出てくる相手の特性を全部並べて、
「反映済み」「影響なし」「未反映（理由つき）」のどれなのかを1行ずつ書いてある。
使用率データを差し替えて**ここに無い特性が出てくるとビルドが警告する**。
判断しないまま表を出さないための仕掛けなので、警告が出たら必ず1行足すこと。
効果を調べるときは `data/abilities_ja.json`（原典は `data/特性の効果.pdf`）を引く。

`their_hit()` は**相手の攻撃特性を反映する**（採用率が最も高い特性＝`threat['ability']`）。
かたいツメ・ちからもち／ヨガパワー・てきおうりょく・テクニシャン・きれあじ・へんげんじざい。
補正の掛け方は `my_hit` と同じ段階に揃えること（威力と攻撃は基礎、一致は stab、残りは extra）。

**かたいツメは接触技だけ1.3倍。** 物理でも じしん・いわなだれ のように接触しない技があるので、
規則で決め打ちせず `party.CONTACT_MOVES` / `NON_CONTACT_MOVES` に技ごとに書く。
かたいツメ持ちが未分類の技を使っているとビルドが警告する。

**まだ入れていないもの:** 自軍の防御特性（今のパーティに軽減特性が無いので影響なし）、
きもったま、バトルスイッチ。`ABILITY_HANDLING` に理由つきで書いてある。

### 使用率データの打ち切り

JSONの `spreads` は各ポケモン**上位12件しか入っていない**。合計は平均71.5%（最小43.8%）にしかならず、
細かい配分が大量に切り捨てられている。`natures` と `items` は上限10件だが選択肢が少ないので
合計はほぼ100%になる。

そのため型の比率は**報告分の合計で割り直した値**を表示している。「全体の何%」ではなく
「判明している配分の中での比率」。この区別を崩さないこと。

### 型の分割ルール

投資量の多い**上位2ステータス**が同じものを1パターンとして統合する。
CS型とSC型のような順序違いは同一視する（これを別物として数えて票が割れるバグを踏んだ）。
第3ステータスへの端数振り（HAS型 vs AS型など）も統合する。

### リージョンフォーム

JSONの各エントリには `region_form` フィールドがある（`samurott-hisui`、`rotom-wash` など）。
図鑑番号だけで形態を決めると、**ヒスイダイケンキが通常ダイケンキの種族値で計算される**。
実際にこのバグを出した。形態の解決は必ず `engine.resolve_form()` を通すこと。

図鑑側の表記は種によって揺れる（`ダイケンキ(ヒスイ)` と `ウォッシュロトム`）ので、
`REGION_KEYWORD` のキーワード照合で吸収している。

### 技名の誤り

`data/ポケモン図鑑.xlsx` には英語名をそのまま音写した誤りが混じっている。
判明分は `engine.MOVE_NAME_FIX` で読み込み時に補正する（技データと使用率シートの両方に適用）。

| xlsx の表記 | 正しい和名 |
|---|---|
| スピリットブレイク | ソウルクラッシュ |
| うでずもう | アームハンマー |

新しい誤りを見つけたらここに追加する。xlsx 側を直せるならそちらが本筋。

### メガ形態と配分の対応

メガ率が100%未満のポケモンで、**メガと逆向きの攻撃方向に振っている型は非メガ運用**とみなす。
カイリューが実例で、メガは A124 < C145 の特殊寄り、非メガは A134 > C100 の物理寄り。
A振り型の比率26%がドラゴナイト非採用率26%と一致する。`generate.build_threats()` に実装済み。

## 表示のルール

- **技の選び方**: 判定（確1 > 乱1 > 確2 …）が最も良い技を主表示する。同判定なら
  `DRAWBACK_MOVES`（はかいこうせん）を避けて他の技を選ぶ。ハイパーボイスで確定1発が取れる相手に
  はかいこうせんを出さないため。2行目に次善の選択肢を出し、判定が上がるなら水色にする。
- **ダメージバー**: 相手のHP全体を幅100%として、濃い部分が最低乱数、薄い部分が最高乱数。
  中央の線が50%。数字を読まずに確1／確2を判断するための主要素なので、壊さないこと。
- **被弾100%以上は赤字**。確定1発される相手を即座に見分けるため。
- **タイプが変換された技には `→フェアリー` のタグを出す**。フェアリースキンが効いているか
  表示だけでは分からず「反映されていないのでは」と疑われたため、目に見えるようにしている。
- 天候・いかく・積みは計算に**含めない**（積み技だけは各行に「+1」を併記）。
  含めない方が素の打点が分かって使いやすい、と実戦で確認済み。
- **ステルスロックだけは例外で、ヘッダの「SR設置済み」トグルで表全体を切り替える。**
  自分が撒いた前提の表と撒いていない表を**両方とも静的に書き出し**、CSSのクラス付け替え
  （`body.sron`）で見せ分ける。**JSでダメージを計算し直さないこと** — JSが動かない環境と
  数値が食い違う。実装は `generate.sr_damage()` と `generate.render_row(hp_eff=...)`。
  - SRダメージは `int(最大HP × いわ相性 / 8)`。マジックガードは無効。**ひこうタイプにも入る**
    （まきびしと違い接地不要）。まきびしを足すなら 1/8・1/6・1/4 で、ひこう・ふゆうには
    入らないので条件が違う。混同しないこと。
  - SR側は判定・%・バーをすべて**残りHPに対する値**にする。こうしないと「バーが満タン＝倒せる」
    が成り立たなくなり、バーの役割が壊れる。
  - HPが減ると主表示する技自体が変わりうる（弱い技で確1が取れる）ので、タグを足すのではなく
    行をまるごと作り直している。切り替えても値が変わらない行は1本にまとめて出力する
    （全行を二重に出すとファイルが倍近くになるため）。
  - 相手側のHPにのみ適用。自分側の被弾%は未計算（相手が撒くかは事前に分からないため後回し）。

## JavaScriptを前提にしないこと

iOSではローカルHTMLでJSが動かない環境がある。そのため**72件すべてのカードを静的に書き出し**、
JSは絞り込みと表示の切り替え（検索・非メガ・SR設置済み）だけを担当する。
**JSに計算をさせないこと。** 出し分けが必要な数値は両方とも書き出しておき、
CSSのクラス付け替えで見せるものを選ぶ。JSが動かない場合は検索欄とトグルが消え、
目次と既定の表（SR無し・非メガ込み）がそのまま読める。
この構造は崩さないこと。新機能を足すときも、JSなしで内容が読める状態を保つ。

## コーディングの約束

- 出力は日本語。ポケモン名・技名・特性名も日本語で統一する。
- 外部CDNを使わない。`index.html` 単体で完結させる（オフラインで開くため）。
- 数値を変えたら必ず `python build/generate.py` を通し、実数値の自動検証が通ることを確認する。
- 新しい計算ロジックを足すときは、既存の `index.html` との差分を取って、
  意図した箇所だけが変わっているか確かめる。
