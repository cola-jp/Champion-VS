# TASKS.md

次に実装したい3機能の仕様。作業前に `CLAUDE.md` を必ず読むこと。
そこに書いてあるドメイン知識（メガ後の実数値、能力ポイント制、丸め順、特性の扱い、
使用率データの打ち切り、リージョンフォーム）は全部この3機能に関係する。

共通の制約:

- **JavaScriptなしで内容が読める構造を崩さない。** 72件のカードは静的に書き出す。
  JSは絞り込みなど「あると便利」な操作だけを担当する。
- **外部CDNを使わない。** `index.html` 単体で完結させる。
- 変更したら `python build/generate.py` を通し、実数値の自動検証が通ることを確認する。
- 既存の `index.html` と差分を取り、意図した箇所だけが変わっているか確かめる。

---

## A. パーティ情報の取り込み

### 目的

今は `build/party.py` を手で編集していて、実際に設定ミスを2回出した
（カバルドンの防御と特防を取り違え、ドリュウズの性格をようきにしてしまった）。
ゲーム画面を見ながら書き写せる形式にして、間違いを機械的に検出できるようにする。

### 入力形式

`party.txt` をリポジトリ直下に置き、1体につき4行。空行区切り。

```
ギャラドス @ ギャラドスナイト
ようき / いかく
171-177-100-72-120-146
たきのぼり / じしん / パワーウィップ / りゅうのまい

エルレイド @ いのちのたま
いじっぱり / きれあじ
169-194-87-76-135-106
せいなるつるぎ / サイコカッター / リーフブレード / かげうち
```

- 1行目: `ポケモン名 @ 持ち物`
- 2行目: `性格 / 特性`
- 3行目: 実数値 `H-A-B-C-D-S`
- 4行目: 技を ` / ` 区切りで最大4つ

### 実数値は「ゲーム画面に表示される値」で書く

**ここが肝。** ゲーム内のステータス画面はメガシンカ前の数値を表示する。
利用者はその表示をそのまま書き写せばよく、メガ後の数値を自分で計算する必要はない。

持ち物がメガストーンなら、ツール側が自動でメガ形態も生成する。
`ギャラドス @ ギャラドスナイト` と書けば「メガ」「非メガ」の2行が出る。
メガストーンとポケモンの対応は `data/ポケモン図鑑.xlsx` の図鑑から引ける
（`メガギャラドス` が存在すればメガ形態あり、と判定できる）。

### 能力ポイントの逆算

実数値・種族値・性格から能力ポイントを逆算する。

```
実数値 = int((2*種族値 + 31 + ポイント*8//4) * 50 // 100) + 5     （HPは +60）
        × 性格補正(1.1 / 0.9)
```

**ポイント1につき実数値がちょうど1上がる**ので、解は一意に決まる。
0〜32を総当たりすれば十分。

以下は全てエラーにして、ビルドを止めること。黙って近い値を採用しない。

- どのポイント数でもその実数値にならない → 性格か実数値の入力ミス
- ポイント合計が66を超える
- 1ステータスが32を超える
- ポケモン名・技名・特性名が図鑑や技データに存在しない

エラーメッセージには「どのステータスが」「入力値がいくつで」「取りうる値が何か」を出す。

### 出力

`build/party.py` の `PARTY` を置き換える形でも、`party.json` を中間生成する形でもよい。
既存の `generate.py` が使っている以下のフラグは、特性名から自動で立てること。

| 特性 | フラグ | 意味 |
|---|---|---|
| かたやぶり | `mold_breaker` | 防御特性を無視 |
| フェアリースキン | `fairy_skin` | ノーマル技→フェアリー、威力1.2倍 |
| きれあじ | `sharpness` | 斬撃技の威力1.5倍 |

`life_orb` は持ち物がいのちのたまなら真。`scarf` はこだわりスカーフなら真。
`boosting_move` は技構成に積み技（りゅうのまい・つるぎのまい・ちょうのまい・めいそう）が
あればその技名。

### 検証

現在の6体を `party.txt` に書き起こして通し、生成された `index.html` が
今のものと**完全に一致する**ことを確認する。1バイトでも差が出たら移植ミス。

---

## B. 使用率データの更新

### 目的

今は2026年7月のデータで固定されている。月が変わったら差し替えられるようにする。

### 現状の問題

**日本語の技名を `ポケモン図鑑.xlsx` の `202607技使用率` シートから引いている。**
このシートは月ごとに作り直す必要があり、JSONだけ差し替えても新しい月の技名が引けない。
シート名もコードに直書きされている。

### 解決策

`data/move_names_en_ja.json` を用意済み（361件）。JSONの英語技名から日本語名を引ける。

```json
{ "power-whip": "パワーウィップ", "spirit-break": "ソウルクラッシュ", ... }
```

これを一次情報にして、xlsxの月次シートへの依存を切る。
シートは補助（対応表に無い技のフォールバック）に格下げする。

対応表に無い技が出てきたら、**警告を出して技名を英語のまま表示する**。
黙って落とすと打点が過小評価される。

### 更新の手順（READMEに書くこと）

1. `data/技使用率データ.JSON` を新しい月のものに差し替える
2. `python build/generate.py` を実行
3. 警告が出たら `data/move_names_en_ja.json` か `data/ポケモン図鑑.xlsx` に追記

取得元は pkmnchamps.com のAPIレスポンス
（`action=list&regulation=reg_mb&month=YYYY-MM&format=singles`）。
ブラウザで開いて保存する運用でよい。自動取得は必須ではない。

### 差し替え時に必ず検出したいこと

- **図鑑に無いポケモンが増えている**（新規解禁）→ 名前を列挙してエラー
- **技データに無い技が使われている** → 技名を列挙してエラー
- リージョンフォームが解決できない → 該当を列挙してエラー

いずれも黙って除外すると、表から特定のポケモンが消えたことに気づけない。

### 変わらない前提

- JSONの `spreads` は各ポケモン上位12件しか入っていない（合計は平均71.5%）。
  型の比率は報告分の合計で割り直す。この扱いは維持する。
- `natures` と `items` は上限10件だが選択肢が少ないので合計はほぼ100%。

### あると嬉しい

前月との比較。順位が大きく動いたポケモンを一覧で出せると、環境の変化に気づける。
`data/` に過去月のJSONを残しておけば実装できる。

---

## C. ステルスロックの有無を切り替える

### 目的

今は素の打点だけで判定している。設置が入っているかで確定数が変わる場面が多く、
実戦では「SRが入っていれば確定1発」という判断を頻繁にする。

### 計算

```
SRダメージ = int(相手の最大HP × いわタイプ相性 / 8)
```

- いわ4倍（リザードン、ウルガモス等）なら最大HPの半分
- **ひこうタイプにも入る**（まきびしと違い、接地していなくても受ける）
- マジックガードは無効化する

まきびしも入れるなら層数で 1/8・1/6・1/4。
こちらは**ひこうタイプとふゆう持ちには入らない**ので条件が違う。混同しないこと。

### 表示方法

**JSなしで読める構造を守る**ため、SR込みの数値は静的に書き出す。

判定が変わるときだけ、打点の行にタグを足すのが良い。

```
はかいこうせん 213-252 ×1.0  確2
  └ SR込み: 確1
```

判定が変わらない場合はタグを出さない。全行に出すとノイズになる。

トグルで全体を切り替えるなら、両方の値をあらかじめ書き出しておいて
CSSのクラス付け替えで見せ分ける。JSで再計算しない。

### 自分側のHPにも効く

被弾の%も、自分がSRを踏んでいれば相対的に重くなる。
ただし相手が設置してくるかは事前に分からないので、**まずは相手側のHPにだけ適用**して、
自分側は後回しでよい。

### 注意

自分のパーティにはキラフロルとカバルドンがステルスロック、キラフロルがまきびしを持っている。
「自分が撒いた場合」の想定が主用途。相手が撒いてくる場合は別軸なので、混ぜないこと。

---

## 優先順位

**A → C → B** を勧める。

Aは他の2つの土台になる（パーティ定義が固まらないと検証しにくい）。
Cは実戦での価値が一番高い。
Bは月が変わるまで急がないが、変わった時に困るので早めに手を付けておくと安心。
